---
variant: fcos
version: 1.4.0
storage:
  files:
    # certs #
    %{~ for cert in values(worker_certs) ~}
    - path: ${cert.path}
      mode: 0644
      contents:
        inline: "${replace(cert.content, "\n", "\\n")}"
    %{~ endfor ~}