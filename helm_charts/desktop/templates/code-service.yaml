{{ $scheme := "http" }}
{{- if .Values.tls.enabled }}
{{ $scheme = "https" }}
{{ end }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "desktop.fullname" . }}-code
  labels:
    app: {{ template "desktop.name" . }}
    chart: {{ template "desktop.chart" . }}
    release: {{ .Release.Name }}
{{- if .Values.codeService.annotations }}
  annotations:
{{ toYaml .Values.codeService.annotations | indent 4 }}
{{- end }}
spec:
  type: {{ .Values.codeService.type }}
{{- if .Values.codeService.clusterIP }}
  clusterIP: {{ .Values.codeService.clusterIP }}
{{- end }}
  ports:
  - name: {{ $scheme }}
    port: {{ .Values.codeService.port }}
    protocol: TCP
{{- if (and (eq .Values.codeService.type "NodePort") ( .Values.codeService.nodePort)) }}
    nodePort: {{ .Values.codeService.nodePort }}
{{- else }}
    targetPort: {{ .Values.ports.desktop }}
{{- end }}
{{- if .Values.codeService.externalIPs }}
  externalIPs:
{{- range $i , $ip := .Values.codeService.externalIPs }}
  - {{ $ip }}
{{- end }}
{{- end }}
  selector:
    app: {{ template "desktop.name" . }}
    release: {{ .Release.Name }}