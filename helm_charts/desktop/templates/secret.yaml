apiVersion: v1
kind: Secret
metadata:
  name: {{ template "desktop.fullname" . }}
  labels:
    app: {{ template "desktop.name" . }}
    chart: {{ template "desktop.chart" . }}
    release: {{ .Release.Name }}
stringData:
  ssh_known_hosts: |
    {{ .Values.sshKnownHosts }}
type: Opaque