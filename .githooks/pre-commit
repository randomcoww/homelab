podman run -it --rm \
  -v $(pwd):$(pwd) \
  -w $(pwd) \
  --entrypoint='' \
  ghcr.io/opentofu/opentofu:latest \
  tofu fmt $(git diff --staged --name-only --diff-filter=AMR | grep -e ".tf$" -e ".tfvars$" -e ".tftest.hcl$")