---
networkd:
  units:
    - name: eth0.network
      contents: |
        [Match]
        Name=eth0

        [Network]
        LinkLocalAddressing=no
        DHCP=yes
systemd:
  units:
    - name: kubelet.service
      enabled: true
      contents: |
        [Unit]
        Description=Kubelet via Hyperkube ACI

        [Service]
        Environment="KUBELET_IMAGE=docker://{{.hyperkube_image}}"
        Environment="RKT_RUN_ARGS=--uuid-file-save=/var/cache/kubelet-pod.uuid \
          --volume=resolv,kind=host,source=/etc/resolv.conf \
          --mount volume=resolv,target=/etc/resolv.conf \
          --insecure-options=image"
        ExecStartPre=/bin/mkdir -p /var/log/containers
        ExecStartPre=-/usr/bin/rkt rm --uuid-file=/var/cache/kubelet-pod.uuid
        ExecStart=/usr/lib/coreos/kubelet-wrapper \
          --allow-privileged=true \
          --manifest-url={{.manifest_url}} \
          --docker-disable-shared-pid=false \
          --make-iptables-util-chains=false
        ExecStop=-/usr/bin/rkt stop --uuid-file=/var/cache/kubelet-pod.uuid
        Restart=on-failure
        RestartSec=5

        [Install]
        WantedBy=multi-user.target
storage:
  files:
    - path: /etc/hostname
      filesystem: root
      mode: 0644
      contents:
        inline:
          {{.hostname}}
    - path: /etc/ssl/certs/ca.pem
      filesystem: root
      contents:
        inline: "{{.tls_ca}}"
    - path: /etc/ssl/certs/ca-key.pem
      filesystem: root
      contents:
        inline: "{{.tls_ca_key}}"

    - path: /etc/ssl/certs/api_server.pem
      filesystem: root
      contents:
        inline: "{{.tls_api_server}}"
    - path: /etc/ssl/certs/api_server-key.pem
      filesystem: root
      contents:
        inline: "{{.tls_api_server_key}}"
    - path: /etc/ssl/certs/controller_manager.pem
      filesystem: root
      contents:
        inline: "{{.tls_controller_manager}}"
    - path: /etc/ssl/certs/controller_manager-key.pem
      filesystem: root
      contents:
        inline: "{{.tls_controller_manager_key}}"
    - path: /etc/ssl/certs/scheduler.pem
      filesystem: root
      contents:
        inline: "{{.tls_scheduler}}"
    - path: /etc/ssl/certs/scheduler-key.pem
      filesystem: root
      contents:
        inline: "{{.tls_scheduler_key}}"

    - path: /etc/ssl/certs/service_account.pem
      filesystem: root
      contents:
        inline: "{{.tls_service_account}}"
    - path: /etc/ssl/certs/service_account-key.pem
      filesystem: root
      contents:
        inline: "{{.tls_service_account_key}}"
passwd:
  users:
  - name: {{.default_user}}
    ssh_authorized_keys:
      - {{.ssh_authorized_key}}
