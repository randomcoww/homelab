{{- if .Values.kasmIngress.enabled -}}
{{- $fullName := include "desktop.fullname" . -}}
{{- $servicePort := .Values.ports.kasm -}}
{{- $ingressPath := .Values.kasmIngress.path -}}
apiVersion: {{ template "desktop.ingress.apiVersion" . }}
kind: Ingress
metadata:
  name: {{ $fullName }}-kasm
  labels:
    app: {{ template "desktop.name" . }}
    chart: {{ template "desktop.chart" . }}
    release: {{ .Release.Name }}
{{- with .Values.kasmIngress.labels }}
{{ toYaml . | indent 4 }}
{{- end }}

{{- with .Values.kasmIngress.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
spec:
{{- if .Values.kasmIngress.ingressClassName }}
  ingressClassName: {{ .Values.kasmIngress.ingressClassName }}
{{- end }}
{{- if .Values.kasmIngress.tls }}
  tls:
  {{- range .Values.kasmIngress.tls }}
    - hosts:
      {{- range .hosts }}
        - {{ . | quote }}
      {{- end }}
      secretName: {{ .secretName }}
  {{- end }}
{{- end }}
  rules:
  {{- range .Values.kasmIngress.hosts }}
    - http:
        paths:
          - path: {{ $ingressPath }}
            pathType: Prefix
            backend:
              service:
                name: {{ $fullName }}-kasm
                port:
                  number: {{ $servicePort }}
      {{- if . }}
      host: {{ . | quote }}
      {{- end }}
  {{- end }}
{{- end }}