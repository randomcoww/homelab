---
variant: fcos
version: 1.4.0
storage:
  files:
    # certs #
    %{~ for cert in values(certs) ~}
    - path: ${cert.path}
      mode: 0644
      contents:
        inline: "${replace(cert.content, "\n", "\\n")}"
    %{~ endfor ~}
    %{~ for cert in values(etcd_certs) ~}
    - path: ${cert.path}
      mode: 0644
      contents:
        inline: "${replace(cert.content, "\n", "\\n")}"
    %{~ endfor ~}
