{
  "customManagers": [
    {
      "customType": "regex",
      "fileMatch": [
        "\\.tf$"
      ],
      "matchStrings": [
        "^\\s*helm_template\\s*=\\s*{[^}]*\\s+repository\\s*=\\s*\"(?<registryUrl>.+)\"\\s+chart\\s*=\\s*\"(?<depName>[\\w-]+)\"\\s+version\\s*=\\s*\"(?<currentValue>[\\d.]+)\""
      ],
      "datasourceTemplate": "helm"
    }
  ]
}